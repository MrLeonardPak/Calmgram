#include "classifier.h"

namespace calmgram::ml::nn {
    void Classifier::Fit(/*calmgram::ml::data::Vectorizer x_train, std::vector<int> y_train*/) {

    }

    bool Classifier::Predict(/*calmgram::ml::data::Vectorizer x_test*/) {
        return true;
    }

    std::vector<float> Classifier::PredictProba(/*calmgram::ml::data::Vectorizer x_test*/) {
        std::vector<float> ret;
        return ret;
    }
}

